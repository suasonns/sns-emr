# Phase 0 — Remediation Change Plan (NOT EXECUTED)

Status: planning document only. No commands in this file have been run.
Database creation and migration execution remain pending explicit authorization.

## 1. Exact `CREATE DATABASE` command required

Run as a Postgres role with `CREATEDB` privilege (the existing `sns_user` role, already
used by all other config files, has this privilege on this instance):

```sql
CREATE DATABASE sns_emr_dev_clean OWNER sns_user;
```

Equivalent CLI form (if a `psql`/`createdb` client is available on PATH — not currently
confirmed on this machine):

```
createdb -h localhost -p 5432 -U sns_user sns_emr_dev_clean
```

## 2. Exact migration command required

From `backend/`, with `DATABASE_URL` exported to point at the new database:

```
$env:DATABASE_URL = "postgresql://sns_user:<password>@localhost:5432/sns_emr_dev_clean"
python -m alembic upgrade head
```

This applies only the 126 already-committed migration files in
`backend/alembic/versions/`. No new migration file is authored or generated by this
plan — forward-only application of existing history, consistent with the repo's own
`backend/scripts/preflight.py` posture ("Forward-only migrations (never rewrite
history)").

## 3. Files that reference `sns_emr_dev_clean`

Config/code (authoritative):
- `backend/alembic.ini` (line 89, `sqlalchemy.url` fallback)
- `backend/dev.env` (untracked, ad hoc)

Tooling/reports referencing it as the dev target (informational, not executable):
- `backend/build_ucier_discovery_packet.ps1`
- `backend/SNS_UCIER_DISCOVERY_PACKET.md`
- `backend/_inventory_files.txt`
- `backend/alembic_history.txt`

Documentation referencing it as the historical/intended dev database:
- `docs/DATABASE_DIFF_REPORT.md`
- `docs/DATABASE_CONNECTION_DIVERGENCE_REPORT.md`
- `docs/LOCATED_PATIENTS_REPORT.md`
- `docs/RESTORE_ANALYSIS_REPORT.md`
- `docs/production/PRODUCTION_READINESS_CLEANUP_CHECKLIST.md`
- `docs/planning/implementation_checkpoint_001.md`
- `docs/owner-platform/PLATFORM_SETTINGS_SPECIFICATION.md`
- `docs/biller-platform/BILLER_PLATFORM_DISCOVERY_REPORT.md`
- `docs/recovery/2026-09-15/build-validation.md`
- `docs/recovery/2026-09-15/FINAL-RECOVERY-REPORT.md`
- `docs/recovery/2026-09-15/OWNER_PLATFORM_VALIDATION_REPORT.md`
- `docs/recovery/2026-09-15/patches/suasonns-psychic-adventure-full.patch`

This session's own Phase 0 deliverables also reference it (expected — they document
the finding):
- `docs/PHASE0_ENVIRONMENT_INVENTORY.md`
- `docs/PHASE0_DATABASE_VALIDATION.md`
- `docs/PHASE0_STARTUP_BLOCKERS.md`

## 4. Expected schema head after migration

Single head: **`f7a8b9c0d1e2`** — "Add relatedness-review workflow_status and
item-level relatedness review tracking to election_addendum_requests /
election_addendum_determinations" (confirmed via `alembic heads` against the current
126-file migration chain; this is the only head — no branching).

After `alembic upgrade head` completes successfully, `sns_emr_dev_clean`'s
`alembic_version` table should contain exactly one row: `f7a8b9c0d1e2`.

## 5. Validation steps after database creation (to run once authorized)

1. `python -m alembic current` (with `DATABASE_URL` pointed at `sns_emr_dev_clean`) — expect `f7a8b9c0d1e2 (head)`.
2. `python -m alembic heads` — expect the same single value, confirming no drift.
3. Start backend: `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000` with `DATABASE_URL` set to `sns_emr_dev_clean` — expect `Application startup complete`, no `RuntimeError`.
4. `Invoke-WebRequest http://localhost:8000/docs` — expect HTTP 200.
5. Start frontend: `npm run dev` in `sns-emr-frontend/` — expect Vite ready, `http://localhost:5173/login` returns HTTP 200 (already independently confirmed working).
6. Attempt a login against a seeded/dev account (if `sns_emr_dev_clean` is freshly created and empty, this will fail until seed data is loaded — a separate, out-of-scope data-seeding step, not part of this remediation plan) or, at minimum, confirm the auth endpoint responds (not "connection refused").
7. Re-run and overwrite `docs/PHASE0_SMOKE_VERIFICATION.md` with the new results once all of the above pass.

## Explicitly out of scope for this remediation

- No new migration file is authored.
- No existing migration is edited.
- No data is seeded, imported, or backfilled as part of this plan (a fresh
  `sns_emr_dev_clean` will be schema-only, no tenant/patient rows, until a
  separate, explicitly authorized seeding step is run).
- No other database (`sns_emr`, `postgres`) is modified or dropped.
- No `dev.env` files are edited or deleted by this plan.
