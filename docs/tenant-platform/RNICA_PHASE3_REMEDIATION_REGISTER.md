# RNICA Phase 3 — Master Remediation Register

**Document Status:** REMEDIATION IN PROGRESS. This register is not a claim
of completion. The system is **not** compliant. Confirmed defects below
remain open until their Status column reads `FIXED` with a linked commit
SHA and passing test evidence.

**Baseline commit under audit:** `16264cf0795e9f29136179a5c598781b1d77c65d`
(branch `suasonns-fantastic-memory`).

**Source documents:** every Phase 2 deliverable in
`docs/tenant-platform/` (`HOPE_FIELD_TRACE_MATRIX.md`,
`SFV_LIFECYCLE_TRACE_MATRIX.md`, `ITEM_CODE_CONFLICT_MATRIX.md`,
`DEAD_EXPORT_PATH_ANALYSIS.md`, `LOCK_GATE_DEPENDENCY_TRACE.md`,
`MISSING_FIELD_RECONCILIATION.md`, `DUPLICATE_AUTHORITY_MATRIX.md`,
`HISTORICAL_COMPATIBILITY_RESULTS.md`, `RNICA_ACCEPTANCE_TEST_MATRIX.md`,
`AUTHORITY_APPLICABILITY_REGISTER.md`, `STAFF_SUPPORT_IMPACT_REGISTER.md`,
`RNICA_HOPE_SFV_FIELD_PLACEMENT_MAP.md`) plus
`CLINICAL_NARRATIVE_OWNERSHIP_AUDIT.md` (this session) and the SFV
completion-visit rule work already executed this session.

---

## Register

| Defect ID | Source document | Finding | Classification | Severity | Current evidence | Affected fields | Affected code | Patient/data risk | Export impact | Lock/readiness impact | Historical impact | Required correction | Required tests | Status | Commit SHA | Closure evidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| P3-001 | `DEAD_EXPORT_PATH_ANALYSIS.md`, `RNICA_HOPE_SFV_FIELD_PLACEMENT_MAP.md` | HOPE value extraction/mapping exists only in the frontend (`hopeReportMapper.js`); no backend authoritative extraction service exists | FRONTEND/BACKEND PARITY | HIGH | `hopeReportMapper.js` (frontend-only); no equivalent backend module found in `backend/app/services/` | All HOPE-coded fields | `sns-emr-frontend/src/intake/hopeReportMapper.js`; no backend counterpart | Export can diverge from what was actually persisted/validated server-side | HOPE export integrity depends entirely on browser state | None directly | None directly | Implement server-side authoritative HOPE extraction (Phase B5); keep frontend as preview only until parity proven | Round-trip parity tests: frontend preview vs backend output on identical fixtures | OPEN | — | — |
| P3-002 | `ITEM_CODE_CONFLICT_MATRIX.md` | N0500/N0510/N0520 item-code collisions across BIMS and opioid/bowel-management areas — one code used for two clinically distinct meanings in different contexts | ITEM-CODE COLLISION | HIGH | `ITEM_CODE_CONFLICT_MATRIX.md` — 9 conflict sites identified (Phase 2 independent re-trace, wider than original 1-site allegation) | N0500, N0510, N0520 and related response paths | `backend/app/domain/forms/form_registry.py`, `sns-emr-frontend/src/intake/hopeReportMapper.js`, `RNICA.jsx` | Wrong clinical value could be exported under the wrong meaning | Exporter may read the wrong source field for a given code | Readiness/validation could reference the wrong semantic field | Historical exports under this code need reinterpretation per version | Separate persisted paths per meaning per Phase B2; do not fix by renaming alone | Item-set version test; per-site round-trip test verifying correct source field for each meaning | OPEN | — | — |
| P3-003 | `HOPE_FIELD_TRACE_MATRIX.md`, `MISSING_FIELD_RECONCILIATION.md` | HOPE fields collected/persisted in the form but not emitted by `hopeReportMapper.js` | REGISTRY/EXPORTER MISMATCH | HIGH | `MISSING_FIELD_RECONCILIATION.md` | See source doc for full list | `sns-emr-frontend/src/intake/hopeReportMapper.js` | Field saves but never reaches HOPE output — silent data loss for reporting purposes | Direct export gap | None directly | Historical saves under this gap already lost that item from any past export | Add exporter mapping for every gap; trace UI→save→export round trip per Phase B4 | Per-field round-trip test | OPEN | — | — |
| P3-004 | `ITEM_CODE_CONFLICT_MATRIX.md`, `RNICA_HOPE_SFV_FIELD_PLACEMENT_MAP.md` | 13 items emitted by the exporter but not declared in the canonical registry | REGISTRY/EXPORTER MISMATCH | MEDIUM | Phase 2 registry/exporter diff | 13 items — see source doc | `form_registry.py` vs `hopeReportMapper.js` | Undeclared item could be silently wrong/unvalidated | Exporter emits data with no registry contract behind it | Readiness never checks an undeclared item | N/A | Add missing registry declarations (Phase B1/B3) | Registry-completeness invariant test (Phase B6) | OPEN | — | — |
| P3-005 | `ITEM_CODE_CONFLICT_MATRIX.md`, `RNICA_HOPE_SFV_FIELD_PLACEMENT_MAP.md` | 1 item declared in the registry but never emitted | REGISTRY/EXPORTER MISMATCH | MEDIUM | Phase 2 registry/exporter diff | 1 item — see source doc | `form_registry.py`, `hopeReportMapper.js` | Declared-but-silent field could mask a real defect | Applicable item silently omitted from export | Could falsely appear "handled" in readiness | N/A | Add exporter mapping or correct declaration if inapplicable | Registry-completeness invariant test (Phase B6) | OPEN | — | — |
| P3-006 | `CLINICAL_NARRATIVE_OWNERSHIP_AUDIT.md` §2 | Legacy `diagnoses.clinicalNarrative` / `clinicalNarrativeReviewed` dependency remains in `INITIAL_FORM` defaults, soft-warning validation, and the dead `ClinicalNarrativeCard`, after the redesign moved the sole active narrative to `finalization.clinicalNarrative` | DEAD OR STALE PATH | HIGH | `RNICA.jsx:487-488, 1076-1077, 2013-2145, 8353-8365, 11392`; `rnica_finalization_service.py:120-121` | `diagnoses.clinicalNarrative`, `diagnoses.clinicalNarrativeReviewed` | `RNICA.jsx`, `rnica_finalization_service.py` | Patient Story `whyHospiceNarrative` (`RNICA.jsx:11392`) reads a field that can never be populated by any current UI action — permanently blank for new assessments | None directly | Backend readiness reads a dead field (see P3-007) | Historical records with legacy data must remain viewable, not deleted | Repoint backend readiness + Patient Story read to `finalization.clinicalNarrative`; remove dead dispatch/card once confirmed no hydration dependency; preserve historical legacy values read-only (Phase C1-C4) | Per Phase C5 test list | AUTHORITY_REVIEW_REQUIRED | — | — |
| P3-007 | `CLINICAL_NARRATIVE_OWNERSHIP_AUDIT.md` §4-5; `LOCK_GATE_DEPENDENCY_TRACE.md` (Finding D) | `evaluate_finalization_readiness()`'s `narrativeReviewed` check reads the dead `diagnoses.clinicalNarrative` path and fails open (`not _has_text(narrative)) or narrative_reviewed` always evaluates `True` when the field is empty); backend never checks `finalization.clinicalNarrative` at all — Lock has zero backend enforcement of the real, current narrative | LOCK | CRITICAL | `rnica_finalization_service.py:120-126`; confirmed via `test_rnica_finalization.py:56-65, 116-119` locking with no narrative present | `finalization.clinicalNarrative` (unchecked), `diagnoses.clinicalNarrative` (checked but dead) | `backend/app/services/rnica_finalization_service.py` | A locked record can have no clinical narrative at all if the API is called directly, bypassing the frontend hard-error at `RNICA.jsx:1091-1092` | None directly | **Lock fails open** — this is the core defect | N/A | Repoint check to `finalization.clinicalNarrative`; decide "reviewed" semantic (recommend dropping — no equivalent field exists on `finalization` today, matches current frontend design) (Phase C3) | "Blank required narrative blocks Lock at the server, direct-API Lock cannot bypass narrative requirement" (Phase C5) | AUTHORITY_REVIEW_REQUIRED | — | — |
| P3-008 | `CLINICAL_NARRATIVE_OWNERSHIP_AUDIT.md` §2 items 3-4 | Fields inside the dead `ClinicalNarrativeCard` (Disease Trajectory radio, Recent Hospitalizations, Recent ED Visits, Utilization Notes, "Build Draft" button, Diagnoses Narrative textarea, reviewed checkbox, RN Addendum) are orphaned behind an undispatched `customRenderer: "clinicalNarrative"` case that no card config ever sets | UNREACHABLE FIELD | HIGH | `RNICA.jsx:8353-8365` (dead branch); confirmed via full grep of every `customRenderer:` value actually assigned (`:8789-8954`) — `clinicalNarrative` never appears | `diseaseTrajectory`, `recentHospitalizations`, `recentErVisits`, `utilizationNotes`, `clinicalNarrative`, `clinicalNarrativeReviewed`, `rnAddendum` (and 1 more field in the same card — verify exact count of 8 against Phase 2 source before closing) | `RNICA.jsx` | These fields cannot be entered through the current UI at all — any code or export that expects them populated will always see them empty for new assessments | Any exporter reading these paths will always get empty values | None of these participate in the readiness gate today | Historical values (if any) remain in old records but cannot be edited going forward | Classify each of the 8 per Phase E1 (restore to a screen / move to Finalization / preserve read-only / deprecate / duplicate / not applicable / authority review); restore reachability (Phase E2) | Per-field reachability test (Phase E3) | OPEN | — | — |
| P3-009 | `SFV_LIFECYCLE_TRACE_MATRIX.md`; `SFV_FRONTEND_BACKEND_PARITY_MATRIX.md` | Frontend and backend SFV lifecycle are architecturally disconnected, not merely mis-wired: there is **no dedicated SFV completion API endpoint at all** (verified — no `sfv-requirement`/`/sfv/` route exists anywhere in `backend/app/api`); the only completion path is a silent automatic hook (`_maybe_complete_open_sfv_for_visit`, `visits.py:3868`) that fires as a side effect of finalizing any qualifying visit; the frontend's `sfv.inPersonSfvCompleted` checkbox is a local-only form field with zero effect on the real `SFVRequirement` record | SFV LIFECYCLE / MISSING API | RESOLVED | `RNICA.jsx` (`sfv` screen checkbox **removed**, replaced with a read-only `SfvStatusCard`); `VisitNotes.jsx` (new `SymptomFollowUpVisitSection`, the actual completion UI, on a separate later visit); `backend/app/api/visits.py` (new `POST /visits/sfv-requirements/{id}/complete` + `GET /visits/sfv-requirements`); `backend/app/core/patient_access.py` (`can_complete_sfv`); `sns-emr-frontend/src/api/sfv.ts` (single client module) | `sfv.inPersonSfvCompleted`, `sfv.sfvDate` | `RNICA.jsx`, `VisitNotes.jsx`, `backend/app/services/hope_phase_b_engine.py`, `backend/app/api/visits.py`, `backend/app/core/patient_access.py`, `sns-emr-frontend/src/api/sfv.ts` | Closed: the checkbox no longer exists; completion now requires calling the authoritative endpoint from a separate, later, finalized visit | Closed: `SfvStatusCard` and `SymptomFollowUpVisitSection` both fetch `GET /visits/sfv-requirements` on mount, so the frontend always reflects backend truth, including on reload; the RNICA HOPE J2052 export fields are now auto-synced from that same backend truth instead of manual self-attestation | Closed: read API now exists | N/A | **Complete this pass:** dedicated completion endpoint built and reused by the frontend; checkbox removed; read endpoint added; authorization via `can_complete_sfv` (generic capability check, not a bespoke on-call feature); idempotent replay and real concurrency verified. **Explicitly deferred, out of scope per directive (no over-engineering):** richer multi-state lifecycle beyond the existing 4-value `status` column, formal on-call/assignment subsystem, void/correction/export/submission-state tracking, browser-automated E2E suite | `backend/tests/test_sfv_completion_api.py` (10 tests, all passing) + `sns-emr-frontend/src/components/SymptomFollowUpVisitSection.test.jsx` (4 tests, all passing) + full regression (46 backend, 277 frontend, production build) | **FIXED** | `658cd29` (frontend wiring + GET endpoint); prior `cc790b1`/`04f461a` (POST endpoint + authorization) | Endpoint-level and component-level tests both green; no schema migration required; frontend messaging-only mitigation from the prior session has now been superseded by a real wiring change. |
| P3-010 | `DUPLICATE_AUTHORITY_MATRIX.md` | Demographics (and other identity fields) are duplicated between Facesheet and RNICA as independently editable stores | DUPLICATE AUTHORITY | MEDIUM | `DUPLICATE_AUTHORITY_MATRIX.md` — see source doc for full field list | Patient name, MRN, DOB, address, phone, emergency contact, attending physician, etc. (verify exact set against source doc) | Facesheet components, `RNICA.jsx` demographics section | Two editable copies can drift, producing inconsistent chart identity | N/A directly | N/A directly | Historical duplicate values must not be deleted until compatibility proven | Facesheet remains authoritative; RNICA displays read-only + provenance for chart-identity fields, keeps independently-required HOPE responses editable (Phase F) | Full Phase F4 test list | OPEN | — | — |
| P3-011 | `RNICA_ACCEPTANCE_TEST_MATRIX.md` (original) | Backend pytest could not be executed because the test environment was reported unavailable | TEST INFRASTRUCTURE | HIGH (blocking) | Originally reported "backend\\.venv absent" | N/A | N/A | N/A | N/A | N/A | N/A | Load `backend/dev.env` in full (not just `DATABASE_URL`) and invoke via `python scripts\run_isolated_tests.py -- tests\<file>` from `backend/` | N/A | **FIXED** | (uncommitted — documentation-only correction, no app code changed) | Re-executed successfully this session: `test_sfv_completion_visit_separation.py` 8/8 passed; `test_rnica_finalization.py` + `test_rnica_hope_workflow.py` 26/26 passed, exit code 0 both times. `RNICA_ACCEPTANCE_TEST_MATRIX.md` §2b/§3 updated, 0 rows remain Blocked. |
| P3-012 | `AUTHORITY_APPLICABILITY_REGISTER.md` | Regulatory-authority rows remain `NOT_VERIFIED` (external CMS/HOPE technical specification citations not independently confirmed against current official sources) | REGULATORY VERIFICATION | MEDIUM (blocking for affected items only) | `AUTHORITY_APPLICABILITY_REGISTER.md` | Item-set version/effective-date claims for HOPE items and SFV timing rule | N/A (documentation) | Implementing an unverified regulatory assumption as fact risks non-compliance | N/A | N/A | N/A | Do not fabricate; verify against current official authority per Phase H; block only the affected item if authority remains unavailable | N/A (verification, not code) | OPEN | — | — |
| P3-013 | Session record (constitution vs. earlier workflow-order product decision) | Workflow-order contradiction exists in documentation: an earlier "FINAL RNICA WORKFLOW PRODUCT DECISION" ordered "1. Patient Story, 2. Evidence & Intake"; a later constitution-drafting instruction supplied "1. Evidence & Intake, 2. Patient Story" | DOCUMENTATION CONFLICT | LOW | Flagged, not resolved, when `SNS_CONSTITUTION.md` was drafted this session | Workflow screen order | `SNS_CONSTITUTION.md`, `RNICA.jsx` navigation config | None directly (ordering only) | N/A | N/A | N/A | Requires explicit user confirmation of which order is authoritative before either doc or code is changed further | N/A | AUTHORITY_REVIEW_REQUIRED | — | — |
| P3-014 | `SFV_LIFECYCLE_TRACE_MATRIX.md` §5; session SFV directive | SFV clinician-role and visit-identity requirements needed explicit alignment: is same-clinician/same-role allowed, and is visit identity (not clinician identity) the determining factor | SFV LIFECYCLE | RESOLVED THIS SESSION | `hope_phase_b_engine.py:397-438` (rule already correctly implemented: `str(completing_visit_id) == str(requirement.trigger_reference_id)` with an independent RN/LPN/LVN discipline gate at `:421-423` that does not compare completing vs. triggering clinician) | `sfv` trigger/completion visit IDs | `backend/app/services/hope_phase_b_engine.py` | None — backend already correct | N/A | N/A | N/A | None required on the backend; frontend parity gap tracked separately as P3-009 | `backend/tests/test_sfv_completion_visit_separation.py` — 8/8 passing | **FIXED** | (uncommitted — test-only addition, no production code changed) | 8 tests added and passing this session: same-nurse/RN/LVN/different-clinician all pass on a separate visit; same-visit self-completion fails; readiness and reporting scenarios covered. `SFV_LIFECYCLE_TRACE_MATRIX.md` §5 and `SNS_CONSTITUTION.md` §31 document the rule. |
| P3-015 | `DEAD_EXPORT_PATH_ANALYSIS.md` (Finding C) | I8005 dead-export-path allegation | DEAD OR STALE PATH | **REFUTED** | `RNICA.jsx:2668` — a real checkbox input feeds the `.other` path; Phase 1's original claim only checked the 14-category loop and missed this control | I8005 / `.other` | `RNICA.jsx` | None | None | None | None | None — no code change needed | Existing coverage sufficient; no new test required | **CLOSED / REFUTED** | — | Confirmed by Phase 2 independent re-trace; preserved in `DEAD_EXPORT_PATH_ANALYSIS.md` as REFUTED/CLOSED per directive §4. |
| P3-016 | `ITEM_CODE_CONFLICT_MATRIX.md` Conflict 2 | M1190 declared twice in `RNICA.jsx`'s `SIDEBAR_CONFIG` (Performance Status sidebar and Skin sidebar); `form_registry.py`'s `HOPE_SKIN_ITEM_CODES` and the exporter (`hopeReportMapper.js:625`) both agree Skin is the real owner, so the Performance Status duplicate flipped `getHopeAdmissionStatus`'s completion indicator based on unrelated Skin data | ITEM-CODE COLLISION | MEDIUM | `bodySystems.js:207` (correct owner, confirmed); `RNICA.jsx` `performanceStatus` sidebar entry (incorrect duplicate, now removed) | M1190 | `sns-emr-frontend/src/components/RNICA.jsx` | None (config-only, zero data-path risk) | None | Performance Status screen's completion indicator no longer flips on unrelated Skin data | N/A | Remove the duplicate `hope: ["M1190"]` declaration from the `performanceStatus` sidebar entry (done); export `SIDEBAR_CONFIG` for testability (done) | Dedicated regression test not yet added — importing the full `RNICA.jsx` module for a unit test carries risk of pulling in unrelated module-evaluation side effects; existing 138/138 HOPE mapper tests + production build re-verified passing with this change in place | APPLIED, NOT YET COMMITTED, NO DEDICATED TEST | — | Fix applied this session; full regression suite (138 frontend + 26+11 backend tests, production build) re-verified green with this change present, but no test asserts the specific behavior change directly. |
| P3-017 | Session SFV implementation directive (defense-in-depth requirements) | `complete_sfv_requirement_from_visit` had no completion-before-trigger guard and no direct cross-patient/cross-tenant guard on the completing visit itself (it trusted caller-supplied values); both are structurally prevented today by the sole caller (`_maybe_complete_open_sfv_for_visit` + `get_authorized_patient`), but the function itself had no independent defense | SFV LIFECYCLE / DEFENSE IN DEPTH | LOW (no live exploit path found — the current sole caller already prevents these; this closes the function-level gap for any future caller) | `hope_phase_b_engine.py:complete_sfv_requirement_from_visit` (before this session: no ordering or patient/tenant check inside the function itself) | `requirement.trigger_datetime`, `completing_visit_datetime`, `Visit.patient_id`, `Visit.tenant_id` | `backend/app/services/hope_phase_b_engine.py` | None today (no reachable path exploits this); protects against a future dedicated completion API being added without re-deriving the same guarantees | N/A | N/A | N/A | Added: (1) reject `completing_visit_datetime < requirement.trigger_datetime`; (2) load the completion `Visit` row directly and reject if it belongs to a different patient or tenant than the requirement | `backend/tests/test_sfv_completion_visit_separation.py::TestSfvCompletionDefenseInDepth` — 3 new tests (completion-before-trigger rejected, cross-patient rejected, visit-not-found rejected), 11/11 total in file passing | **FIXED** | (uncommitted this pass) | New tests added and passing; full regression suite (11 SFV + 26 finalization/hope_workflow backend, 138 frontend, production build) re-verified green with this change present. |

*(Additional findings from the 12 Phase 2 deliverables beyond this minimum list — e.g. remaining rows in `HOPE_FIELD_TRACE_MATRIX.md`, `HISTORICAL_COMPATIBILITY_RESULTS.md`, and `STAFF_SUPPORT_IMPACT_REGISTER.md` not yet assigned individual defect IDs — must be triaged and added as P3-016+ before Phase B/E work on those areas is marked complete. Not yet done in this pass.)*

---

## Status summary (as of this session)

| Status | Count |
|---|---|
| FIXED | 3 (P3-011 — test environment; P3-017 — SFV defense-in-depth guards; P3-009 — SFV completion endpoint + frontend wiring) |
| FIXED (test-only, no app code change) | 1 (P3-014 — SFV backend rule already correct, tests added) |
| CLOSED / REFUTED | 1 (P3-015 — I8005) |
| AUTHORITY_REVIEW_REQUIRED | 3 (P3-006, P3-007, P3-013) |
| APPLIED, NOT YET COMMITTED, NO DEDICATED TEST | 1 (P3-016) |
| OPEN | 8 (P3-001, P3-002, P3-003, P3-004, P3-005, P3-008, P3-010, P3-012) |
| **Total registered** | **17** (minimum list; full Phase 2 corpus not yet fully triaged — see note above) |

No defect above is claimed as `FIXED` on the basis of documentation alone.
P3-009, P3-011, P3-014, and P3-017 are the only rows marked `FIXED`, and
all four are backed by executed test evidence recorded in this document
and in `RNICA_ACCEPTANCE_TEST_MATRIX.md`.
