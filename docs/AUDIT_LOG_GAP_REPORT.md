# Audit Log Gap Report

Scope: Audit Logs, including Event History, Staff Activity, Security Events, Permission Changes,
Agency Actions, Filtering, Search, Export. No redesign, no new architecture. Classification:
COMPLETE / PARTIAL / MISSING.

| Item | Frontend | Backend | Database | API | Test | Classification |
|---|---|---|---|---|---|---|
| **Event recording** | N/A (display only) | Existing audit-log write path (pre-existing on `main`), extended in recovery with `_severity_for_event`/`_affected_permissions_for_role_change` | Existing audit log table, extended with `entity_type`/`entity_id`/`request_id`/`severity`/`affected_permissions` fields | `list_audit_logs()` in `owner_admin.py` | `test_owner_audit_logs.py` — 34/34 passing | **COMPLETE** |
| **Staff activity events** | Displayed in `AuditLogs.jsx`/`AuditEventDrawer.jsx`; per-account audit tab in `StaffProfileDrawer.jsx` | Staff lifecycle/role/permission-grant actions all write audit events (create/status-change/role-change/grant/revoke) | Same audit table | `get_platform_staff_audit_history` endpoint | Exercised in `test_owner_platform_staff_lifecycle.py`/`test_owner_platform_staff_delegation.py` (audit-history assertions) | **COMPLETE** |
| **Security events** | Severity badges in `AuditLogs.jsx`/`AuditEventDrawer.jsx` | `_severity_for_event()` classifies events (flat table + context-aware tenant-status and role-change severity) | `severity` field on audit records | Returned per audit-log row | `test_severity_for_event_flat_table`, `test_severity_for_event_tenant_status_is_context_aware`, `test_severity_for_event_role_change_is_context_aware`, and 2 more severity tests — all passing | **COMPLETE** |
| **Permission changes tracked** | `AuditEventDrawer.jsx` renders before/after role diffs | `_affected_permissions_for_role_change()` computes the real capability diff between old and new role | Stored in `affected_permissions` on the role-change audit record | Returned only for `OWNER_CHANGED_STAFF_ROLE` events | `test_affected_permissions_computes_real_capability_diff`, `test_affected_permissions_none_for_missing_or_equal_roles`, `test_affected_permissions_none_for_unrecognized_role_value`, `test_audit_logs_role_change_event_carries_severity_and_affected_permissions`, `test_audit_logs_non_role_change_event_has_no_affected_permissions` — all passing | **COMPLETE** |
| **Agency (tenant) actions tracked** | Existing tenant-management audit flows (pre-existing on `main`, not part of the 46 recovered files) | Tenant onboarding/status/financials actions already write audit events prior to this recovery; recovery only added severity/entity-filter awareness on top | Same audit table | Same `list_audit_logs()` endpoint, now with richer filters | Tenant-status severity is explicitly tested (`test_severity_for_event_tenant_status_is_context_aware`, `test_severity_for_event_tenant_status_missing_metadata_defaults_info`) | **COMPLETE** (agency-action recording itself predates this recovery; recovery adds richer classification on top, and that addition is tested) |
| **Filtering** | Category, tenant, date-range filters wired in `AuditLogs.jsx` | `entity_type`, `entity_id`, `request_id`, `severity` filters added to `list_audit_logs()` | N/A (query-level filtering) | Implemented | `test_audit_logs_entity_id_filters_to_exact_entity`, `test_audit_logs_entity_id_composes_with_category_filter` — passing | **COMPLETE** |
| **Search** | Debounced search input (`searchInput`/`search` state) wired into the audit-log fetch call | Existing search parameter on `list_audit_logs()` (pre-existing, not newly added by recovery) | N/A | Implemented | Covered indirectly (search composes with filters in the entity-id composition test); no test found targeting free-text search matching specifically | **PARTIAL** (functionality present and wired end-to-end, but no dedicated test isolates free-text search behavior on its own) |
| **Export** | `handleExport()` + `downloadCsv()` in `AuditLogs.jsx` — generates a timestamped CSV client-side from the currently filtered result set | Uses the same `list_audit_logs()` endpoint (no separate export endpoint; export is a client-side CSV serialization of the fetched rows) | N/A | Implemented (reuses list endpoint) | **No backend or frontend test found for the export/CSV-generation path** | **PARTIAL** (feature exists and is wired, but is client-side-only with no server-side export endpoint and no automated test coverage) |
| **Validation** (audit log data shape, severity/affected-permissions correctness) | N/A | Covered by the severity/affected-permissions test set above | N/A | N/A | 34/34 in `test_owner_audit_logs.py` | **COMPLETE** |
| **Standalone Audit Logs page design status** | `AuditLogs.jsx`, `AuditEventDrawer.jsx`, `auditCategories.js` implemented | N/A | N/A | N/A | N/A | Recovered checkpoint doc (`SNS_STAFF_ACCESS_CHECKPOINT.md`) states this page was "WAITING FOR DESIGN REVIEW" as of its last note, but the directive states Audit Logs "already has approved design" — **this status conflict is not resolved by code inspection**; flagging as a fact, not re-litigating it |
| **Frontend build/typecheck/lint** | N/A | N/A | N/A | N/A | Never run against the recovery worktree | **MISSING** (validation gap) |
| **Merge to `main`** | N/A | N/A | N/A | N/A | N/A | **MISSING** — exists only in the isolated recovery branch; the live `main` Audit Logs page is still the original, unrecovered version |

## Overall Audit Logs classification: **PARTIAL**

6 of 8 requested completion criteria (events recorded, staff activity, security events,
permission changes, agency actions, filtering) are COMPLETE with passing tests. 2 are PARTIAL:
**search** (implemented, functionally wired, but not independently test-covered) and **export**
(implemented client-side, functionally wired, but has no server-side endpoint or automated test).
As with Staff Management, nothing here has been merged to `main` or frontend-built/validated yet.
