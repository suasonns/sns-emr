# ACCESS_CONTROL_CURRENT_STATE.md

**Status:** Evidence review only. No code, schema, migration, RBAC, permission,
tenancy, or audit behavior was changed to produce this document.

**Terminology:** `ACP` = Advance Care Planning (unchanged, per
`PHASE2_TERMINOLOGY_DECISION.md`). "Access Control Policy" is never
abbreviated as `ACP` anywhere in this document.

---

## Evidence Table

| Control area | Repository evidence | Implementation location | Test evidence | Status | Gap |
|---|---|---|---|---|---|
| Authentication | JWT bearer token, HS256, issuer/audience-checked, access vs. refresh token typing enforced | `backend/app/core/security.py` (`create_access_token`, `decode_access_token`, `get_current_user`) | `backend/tests/test_auth_hardening.py`, `test_session_stability.py`, `test_password_setup_flow.py` | IMPLEMENTED AND TESTED | **Duplicate implementation**: a second, independent `CurrentUser`/`get_current_user` pair exists in `backend/app/core/auth.py` (separate `VALID_ROLES` set, separate token-decode path calling the same `decode_access_token`); `backend/app/dependencies/auth.py` wraps `core.auth.get_current_user`; `backend/app/security/deps.py` is a **dev-only, no-auth stub** (`get_current_user_id` returns a random UUID, "Does NOT enforce authentication"). Three distinct code paths answer "who is the current user." |
| Role hierarchy | `ROLE_AUTHORITY_RANK` (rank 0 = `OWNER`, ascending = less senior) | `backend/app/core/roles.py:299` | `backend/tests/test_owner_platform_rbac_matrix.py` (20 tests) | IMPLEMENTED AND TESTED | None found for the platform-role hierarchy itself. Tenant-side clinical role seniority is not rank-based (see Permission evaluation row). |
| Authority evaluation | `role_authority_rank()`, `is_owner_role()`, `is_platform_role()` | `backend/app/core/roles.py:188,193,331` | Covered indirectly by RBAC matrix tests | IMPLEMENTED AND TESTED | None found |
| Permission evaluation | Three parallel mechanisms coexist: (1) `role_can()` + `PLATFORM_PERMISSION_MATRIX` (platform/owner surface), (2) `has_capability()`/`ROLE_CAPABILITIES` in `core/capabilities.py` (clinical patient-access surface), (3) `has_permission()` in `core/permissions.py` | `backend/app/core/roles.py:376,502`; `backend/app/core/capabilities.py`; `backend/app/core/permissions.py` | Mechanisms (1) and (2): tested (`test_owner_platform_rbac_matrix.py`; capability logic exercised via `patient_access.py` consumers). Mechanism (3): **no test found** | CONFLICTING | **`has_permission()` (`core/permissions.py`, final function) is a hardcoded stub: `return True` unconditionally**, regardless of arguments. It is actively called at 5 sites in `backend/app/api/survey.py` (lines 39, 81, 93, 105, 125) guarding chart-PDF download, compliance viewing (×3), and data export — **these checks currently always pass for any authenticated user, granting no actual restriction.** `require_permission()` in the same file is also a placeholder that returns the user unconditionally without checking anything. |
| Route authorization | `require_roles()` (clinical), `require_platform_permission()` (platform/owner), `require_capability()`/`require_any_capability()` (clinical capability) | `backend/app/core/permissions.py`; `backend/app/core/role_guards.py`; `backend/app/core/capabilities.py` | `test_owner_platform_rbac_matrix.py` for platform routes | CONFLICTING | 1) `backend/app/api/audit_dashboard.py:85` calls `require_roles(["ADMIN", "DPCS", "QA"])` — `"ADMIN"` normalizes to `ADMINISTRATOR` correctly, but **`"QA"` does not normalize to any issued role** (`QA_MANAGER`/`QA_REVIEWER`/`COMPLIANCE_OFFICER` are the only real QA roles, none aliased to `"QA"`) and is not in `CLINICAL_ADMIN_ROLES`, so **no real QA staff account can ever satisfy this gate** — verified by reading `_ALIASES` and `QA_ROLES` in `core/roles.py`. 2) `backend/app/api/adr_exports.py::export_adr` (`POST /chart/export/adr`) has **no `Depends(get_current_user)` and no role/permission check of any kind** — confirmed by reading the full file; the endpoint accepts any request and returns a PHI-bearing PDF (ADR packet or deficiency report) for any `patient_id` supplied in the request body, with no tenant filter applied in `AdrAuditService.run_full_audit` (confirmed no `tenant` references in `adr_audit_service.py`). |
| Service-layer authorization | `get_authorized_patient()` — two-layer check (tenant + care-team assignment) | `backend/app/core/patient_access.py` | Consumed by `backend/app/api/documents.py`; **no dedicated unit test found for `patient_access.py` itself** | IMPLEMENTED, NOT TESTED (directly) | Not confirmed to be used by every patient-scoped router — only verified for `documents.py` in this pass; other routers may query `Patient` directly without this helper (not verified either way — **NOT VERIFIED**). |
| Frontend permission handling | `RequireRoleAccess.tsx` (route guard), `authorization.ts` (`hasRouteAccess`, `hasFeatureAccess`, `canAccessPath`) | `sns-emr-frontend/src/components/RequireRoleAccess.tsx`, `sns-emr-frontend/src/utils/authorization.ts` | **No frontend authorization test suite found** | IMPLEMENTED, NOT TESTED | Frontend checks are coarse (`access_scope` in `{platform, billing, tenant}`), not capability-level — by design, backend enforces per-action RBAC (per in-code comment); consistent with a client-side-is-not-the-authority model, not itself a gap. |
| Tenant isolation | Global SQLAlchemy `do_orm_execute`/`before_flush` event listeners: auto-filter all `SELECT`s on `tenant_id`, auto-stamp new rows, block cross-tenant `INSERT`/`UPDATE`/`DELETE` | `backend/app/core/tenant_orm_filters.py`; marker mixin `backend/app/models/tenant_mixin.py` | **No dedicated automated test found exercising `tenant_orm_filters.py` directly** (several test files reference `tenant` concepts but none was confirmed to specifically test the ORM-level ISOLATION mechanism itself — NOT VERIFIED further in this pass) | IMPLEMENTED, NOT VERIFIED (test coverage) | A documented "SUPER ADMIN MODE" bypasses all tenant filtering/write-protection when `get_current_tenant()` returns `None` — this is an intentional escape hatch (logged via `logger.warning`), not a bug, but its own authorization gate (who/what can cause `get_current_tenant()` to return `None`) was **not traced in this pass — NOT VERIFIED**. |
| Resource-level authorization | `get_authorized_patient()` (see Service-layer row); Physician Identity Mapping gate (`is_provider_identity_role`, `is_identity_verified`, `is_tenant_wide_oversight_role`) | `backend/app/core/patient_access.py`; `backend/app/services/physician_identity_service.py` | `backend/tests/test_physician_identity_mapping.py` | IMPLEMENTED AND TESTED (for the physician-identity path); IMPLEMENTED, NOT TESTED (for the base assignment-scoping path) | None found beyond the "not confirmed universal" note above |
| Patient-record restrictions | Same as Resource-level row | Same | Same | Same | Same |
| Owner/admin restrictions | `is_owner_role()`, `require_owner()`, `CLINICAL_ADMIN_ROLES` exclusion of `OWNER`/`PLATFORM_ROLES` from clinical/financial fallback | `backend/app/core/roles.py`; `backend/app/core/role_guards.py` | `test_owner_platform_rbac_matrix.py` | IMPLEMENTED AND TESTED | None found |
| Allowed-action auditing | `log_event()` helper, `AuditLog` model, `_safe_log_event()` wrapper used at 16 call sites in `visits.py` | `backend/app/services/audit_logger.py`; `backend/app/models/audit_log.py`; `backend/app/api/visits.py` | `backend/tests/test_audit_logging.py`, `test_owner_audit_logs.py` | IMPLEMENTED AND TESTED (where wired); PARTIAL overall | Confirmed gap (already tracked in `PHASE2_EXECUTION_PACKAGE.md` Workstream 3): the 5 core RNICA CRUD endpoints (`save`, `GET`, `PUT`, `DELETE`, `lock`) do not call `_safe_log_event()`. |
| Denied-action auditing | — | — | — | **NOT FOUND** | No call site was found where a 403/401 denial itself is written to `AuditLog` (only successful/allowed actions are logged where audit logging exists at all). Repository-wide search for a denial-audit pattern returned no matches. |
| Export/report authorization | Mixed: `documents.py` uses `get_authorized_patient()` (tenant + care-team check); `admin/chart_export.py` uses a local `require_admin()`; `adr_exports.py` uses **no check at all** (see Route authorization row); `survey.py` uses the non-functional `has_permission()` stub | `backend/app/api/documents.py`, `backend/app/api/admin/chart_export.py`, `backend/app/api/adr_exports.py`, `backend/app/api/survey.py` | Partial — not exhaustively enumerated for every export/report route in this pass | CONFLICTING | No single, consistent authorization pattern across export/report endpoints; at least one (`adr_exports.py`) is fully unauthenticated and at least four (`survey.py`) rely on a stub that always passes. |
| Background-job authorization | — | — | — | NOT FOUND | No Celery/cron/background-task authorization mechanism was found in this pass (no scheduler/task-queue authorization code located). |
| Service-account authorization | — | — | — | NOT FOUND | No `ServiceAccount`/API-key-based authorization mechanism was found for backend-to-backend calls. |
| Emergency access | — | — | — | NOT FOUND | No break-glass/emergency-access code, model, or documentation was found anywhere in the repository. |

---

## Notes on Scope of This Pass

- This review inspected the specific files and functions cited above by
  opening and reading their implementations, not by inferring behavior from
  filenames or comments.
- Rows marked NOT FOUND reflect an active, targeted search across
  `backend/app`, `backend/tests`, `backend/alembic`, and `docs/` for the
  named concept with no matching implementation — not an assumption of
  absence.
- Rows marked NOT VERIFIED reflect areas that exist but whose full test
  coverage or downstream consumers were not exhaustively traced in this
  pass; a broader pass could change these classifications.
