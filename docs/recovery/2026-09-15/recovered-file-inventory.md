# Recovered File Inventory

Recovered source for every entry below: live worktree `C:\dev\SNS EMR\copilot-worktrees\sns-emr\suasonns-psychic-adventure`, on branch `suasonns-psychic-adventure`, with preserved content from commit `6f3c8ac4c2e60047721d934eaf9c77c22f18148d` plus uncommitted changes. Status below is relative to current `main` (`b552fcb0e90d8eca25beacb55c5b35d3d575d1ee`) because that is the review base used in the isolated recovery worktree.

## Documentation

- `.github/copilot-instructions.md` — **new**. Purpose: repository-wide Copilot operating policy for SNS EMR, including mandatory startup protocol, non-destructive editing rules, and architecture constraints; the file head explicitly states it applies to “every GitHub Copilot chat, agent session, automated task, code review, implementation task, debugging task, and repository operation.” Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 11:53:46 -07:00`. Dependencies: not imported by application code; consumed operationally by Copilot sessions and references repository docs/policies.
- `docs/PLATFORM_ARCHITECTURE_ROADMAP.md` — **new**. Purpose: planning document that defines the “authoritative development order,” establishes Platform Owner Experience as the active phase, and enumerates roadmap sections for Platform Owner User Management, Tenant Experience, Biller Experience, shared RBAC architecture, revenue oversight, and field-testing gates. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 16:06:29 -07:00`. Dependencies: referenced conceptually by the recovered RBAC/staff-management implementation; its platform-role and safeguard sections align with `backend/app/core/roles.py`, `backend/app/api/owner_admin.py`, and the owner-portal staff UI.
- `docs/SNS_STAFF_ACCESS_CHECKPOINT.md` — **new**. Purpose: in-session checkpoint note that freezes scope for SNS Staff & Access, lists completed workflows, describes ownership continuity and delegation goals, records migration chain, and inventories the recovered modified/untracked files plus known limitations and explicitly deferred work. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 22:53:24 -07:00`. Dependencies: documentary companion to the recovered code; references most backend/frontend files in this recovery set and maps them to the intended Platform Owner phase.

## Database Migrations

- `backend/alembic/versions/um2s1a2f3f4b5_add_department_notes_updated_by_to_users.py` — **new**. Purpose: first migration in the recovered SNS Staff & Access chain; adds `department`, `notes`, and `updated_by` to `users`, and creates `fk_users_updated_by_users`, matching the new profile-edit tracking fields later used by the owner staff endpoints. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 16:10:33 -07:00`. Dependencies: revises `pay3v4e5r6i7f`; supports `backend/app/models/user.py` and profile mutations in `backend/app/api/owner_admin.py`; superseded by later chain members `um2a...`, `um2b...`, `um2c...`, and `um2d...`.
- `backend/alembic/versions/um2a1c2c3o5u6_add_account_type_to_users.py` — **new**. Purpose: adds `users.account_type` with check constraint `ck_users_account_type_valid` for `HUMAN_STAFF`, `SERVICE_ACCOUNT`, `AUTOMATION_ACCOUNT`, and `API_CLIENT`, defaulting existing rows to `HUMAN_STAFF`. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 16:44:33 -07:00`. Dependencies: revises `um2s1a2f3f4b5`; matches `backend/app/core/account_types.py`, `backend/app/models/user.py`, and account-type filters/identity tabs in the owner staff UI.
- `backend/alembic/versions/um2b1c2c3o5u7_add_identity_fields_to_users.py` — **new**. Purpose: adds `responsible_owner_id`, `identity_purpose`, and `identity_scope` to `users`, with `ix_users_responsible_owner_id`, so service accounts and API clients can carry accountable human-owner, purpose, and scope metadata. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 18:53:19 -07:00`. Dependencies: revises `um2a1c2c3o5u6`; supports `backend/app/models/user.py`, validation in `backend/app/api/owner_admin.py`, and frontend identity-accountability forms in `AddStaffModal.jsx` and `StaffProfileDrawer.jsx`.
- `backend/alembic/versions/um2c1c2c3o5u8_add_platform_to_users.py` — **new**. Purpose: adds a first-class `users.platform` column, defaulting to `SNS Hospice Solutions`, to implement the hierarchy “Platform > Department > Job Title > Platform Role > Access Level” described in the roadmap and checkpoint docs. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 19:20:19 -07:00`. Dependencies: revises `um2b1c2c3o5u7`; pairs with `backend/app/core/platforms.py`, `backend/app/models/user.py`, and platform selection/filtering in the staff UI and owner API.
- `backend/alembic/versions/um2d1c2c3o5u9_add_staff_status_and_permission_grants.py` — **new**. Purpose: final recovered migration; adds `users.platform_staff_status` and creates `staff_permission_grants` with audit fields for delegated `staff.*` capabilities, explicitly keeping lifecycle status separate from the global `users.active` auth gate. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 20:52:06 -07:00`. Dependencies: revises `um2c1c2c3o5u8`; supports `backend/app/models/staff_permission_grant.py`, role-union logic in `backend/app/core/roles.py`, permission endpoints in `backend/app/api/owner_admin.py`, and delegation tests.

## Backend API

- `backend/app/api/owner_admin.py` — **modified**. Purpose: still contains owner tenant onboarding, financial assignment, system health, and adoption-health logic, but the recovered version also turns the owner API into the authoritative SNS Staff & Access backend: it adds audit severity/affected-permissions derivation, platform-staff-only roster/list/detail/profile/role/status/remove/revoke/password-reset endpoints, delegated-permission grant endpoints, and platform-staff serializers that expose derived access levels, capabilities, and lifecycle state. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-15 01:18:22 -07:00`. Dependencies: imports `account_types.py`, `departments.py`, `job_titles.py`, `platforms.py`, `protected_tenants.py`, `role_guards.py`, `roles.py`, `models/user.py`, and `models/staff_permission_grant.py`; consumed by `sns-emr-frontend/src/api/ownerAdmin.ts` and exercised by all recovered backend tests.

## Account Types

- `backend/app/core/account_types.py` — **new**. Purpose: defines the validated account-type taxonomy (`HUMAN_STAFF`, `SERVICE_ACCOUNT`, `AUTOMATION_ACCOUNT`, `API_CLIENT`) and `normalize_account_type`, with docstrings stating the module distinguishes human staff from non-human platform identities on the shared `users` table. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 16:44:16 -07:00`. Dependencies: imported by `backend/app/api/owner_admin.py`; represented in `backend/app/models/user.py`, Alembic migration `um2a...`, the owner API response `available_account_types`, and the owner UI scopes/forms.

## Authentication

- `backend/app/core/auth.py` — **modified**. Purpose: retains JWT decoding and `CurrentUser` construction, but the recovered change expands `VALID_ROLES` so the new platform staff roles (`PLATFORM_ADMIN`, `PLATFORM_SECURITY`, `PLATFORM_DEVELOPER`, `PLATFORM_DEVOPS`, `PLATFORM_IMPLEMENTATION`, `PLATFORM_CUSTOMER_SERVICE`, `PLATFORM_QA`, `PLATFORM_AUDITOR`) can authenticate successfully instead of failing post-login with `403 Invalid role`. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 22:10:00 -07:00`. Dependencies: imports `decode_access_token` from `app.core.security`; called anywhere `get_current_user` is used, including `backend/app/api/owner_admin.py` and `backend/app/core/role_guards.py`; behavior covered indirectly by the recovered owner-role and platform-role tests.

## Departments

- `backend/app/core/departments.py` — **new**. Purpose: declares the 12 platform departments (`EXECUTIVE`, `PLATFORM_ADMINISTRATION`, `CUSTOMER_SUPPORT`, `SECURITY`, etc.) and `normalize_department`, with explicit docstrings that department is organizational only and grants no permissions. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 20:00:23 -07:00`. Dependencies: imported by `backend/app/api/owner_admin.py`; surfaced to the frontend in `available_departments`, used by `AddStaffModal.jsx`, `StaffProfileDrawer.jsx`, and `UserManagement.jsx`, and backed by `users.department` in `backend/app/models/user.py`.

## Job Titles

- `backend/app/core/job_titles.py` — **new**. Purpose: defines department-scoped job-title catalogs and `normalize_job_title`, allowing catalog validation where a department is modeled and free-text fallback where a department has no curated catalog yet; the module docstring explicitly keeps titles presentation-only, not authorization-bearing. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 20:00:30 -07:00`. Dependencies: imported by `backend/app/api/owner_admin.py`; its `job_titles_by_department` response drives cascading selectors in `AddStaffModal.jsx`, `StaffProfileDrawer.jsx`, and `UserManagement.jsx`; persisted via `users.job_title` in `backend/app/models/user.py`.

## RBAC

- `backend/app/core/protected_tenants.py` — **modified**. Purpose: still guards permanent tenants from purge/delete scripts, but the recovered change extracts `PLATFORM_TENANT_ID` as a named constant for `SNS Hospice Solutions` so staff-creation/profile code can use one canonical platform-tenant UUID instead of duplicating the literal. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:11:23 -07:00`. Dependencies: imported by `backend/app/api/owner_admin.py` and recovered backend tests; continues to own `PROTECTED_TENANT_IDS` and `assert_not_protected` for scripts/tools.
- `backend/app/core/role_guards.py` — **modified**. Purpose: keeps `require_owner` unchanged and adds `require_platform_permission(capability)`, a deny-by-default FastAPI dependency that first requires a platform role and then checks capability via `role_can`, enabling non-owner platform roles to reach owner endpoints only when the central RBAC map allows it. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:11:23 -07:00`. Dependencies: imports `is_owner_role`, `is_platform_role`, and `role_can` from `roles.py`; used heavily by `backend/app/api/owner_admin.py`; exercised directly in `backend/tests/test_owner_platform_rbac_matrix.py`.
- `backend/app/core/roles.py` — **modified**. Purpose: preserves the repo’s canonical role aliasing and access-scope helpers, then adds the recovered SNS Staff & Access RBAC foundation: expands `PLATFORM_ROLES`, defines `ACCESS_LEVEL_FOR_ROLE`, `PLATFORM_STAFF_STATUSES`, `STAFF_CAPABILITIES`, `PLATFORM_PERMISSION_MATRIX`, `effective_capabilities_for_role`, `capabilities_for_role`, `role_can`, and `can_delegate_capability`, including owner ceilings, non-delegable owner assignment, and delegated-grant union behavior. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:11:23 -07:00`. Dependencies: imported by `backend/app/api/auth.py`, `backend/app/billing/*`, `backend/app/core/role_guards.py`, `backend/app/api/owner_admin.py`, and multiple tests; referenced conceptually by `staff_permission_grant.py` and the frontend access-level/actor-capabilities surfaces.

## Database Models

- `backend/app/models/__init__.py` — **modified**. Purpose: central model auto-loader remains intact, but the recovered edit adds `from app.models.staff_permission_grant import StaffPermissionGrant` so Alembic metadata and runtime model registration include the new delegated-permission table. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:11:23 -07:00`. Dependencies: depends on the new `staff_permission_grant.py`; imported implicitly by migration/autoload paths that walk `app.models`.
- `backend/app/models/user.py` — **modified**. Purpose: extends the existing user model with SNS Staff & Access fields: `department`, `notes`, `updated_by`, `account_type`, `responsible_owner_id`, `identity_purpose`, `identity_scope`, `platform`, and `platform_staff_status`, while preserving existing auth, SSN, and physician-linkage fields and keeping tenant isolation on `tenant_id`. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:11:23 -07:00`. Dependencies: imported throughout the backend; new fields are used by `backend/app/api/owner_admin.py`, migrations `um2s...` through `um2d...`, test fixtures in the recovered backend tests, and owner-portal staff/profile screens via API serialization.

## Permission Grants

- `backend/app/models/staff_permission_grant.py` — **new**. Purpose: models one delegated `staff.*` capability grant per row, including who granted it, when, why, optional expiration, and revocation metadata; the module docstring explicitly says rows add capability on top of role defaults and never define capability semantics themselves. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 20:51:50 -07:00`. Dependencies: imported by `backend/app/api/owner_admin.py`, registered through `backend/app/models/__init__.py`, referenced by `backend/app/core/roles.py` docstrings and active-grant logic, created by migration `um2d...`, and validated in `backend/tests/test_owner_platform_staff_delegation.py`.

## Platform Owner

- `backend/app/core/platforms.py` — **new**. Purpose: defines the canonical platform catalog and `normalize_platform`, defaulting to `SNS Hospice Solutions` while listing `SNS Home Health Solutions` and `SNS Scribe` as future-known platforms so the owner staff hierarchy can model platform assignment before multi-platform staffing exists. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 19:20:12 -07:00`. Dependencies: imported by `backend/app/api/owner_admin.py`; surfaced as `available_platforms` to `UserManagement.jsx`, `AddStaffModal.jsx`, and `StaffProfileDrawer.jsx`; persisted in `backend/app/models/user.py` via migration `um2c...`.
- `backend/app/services/admin_bootstrap_service.py` — **modified**. Purpose: keeps the development-login bootstrap service intact but changes the configured platform-owner display name from `Development Platform Owner` to `SNS Tech Solutions`, aligning seeded owner identity naming with the platform branding used throughout the recovered owner UI. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:37:07 -07:00`. Dependencies: imports `Tenant`, `User`, and `hash_password`; called by backend startup/seed flows that provision development identities.
- `sns-emr-frontend/src/owner/OwnerDashboard.jsx` — **modified**. Purpose: owner shell/controller component now wraps owner pages in the new `OwnerShell`, keeps real `fetchOwnerDashboard` loading for summary pages, and explicitly exempts the recovered `users` and `audit` tabs from blocking on owner-only summary data so delegated platform staff can reach their permitted surfaces. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 22:12:44 -07:00`. Dependencies: imports owner pages, `fetchOwnerDashboard`, `fetchOwnerTenants`, `getCurrentUser`, `hasRouteAccess`, theme hooks, and `OwnerShell`; it is routed by `sns-emr-frontend/src/App.tsx`.
- `sns-emr-frontend/src/owner/shell/OwnerShell.jsx` — **new**. Purpose: Figma-style owner-platform chrome with icon-only sidebar, status topbar, theme toggle, and sign-out button; it only wraps children and leaves actual owner page content unchanged, making it the visual container for the recovered owner-platform experience. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:11:23 -07:00`. Dependencies: imports `BrandLogo`, shell icons, and `tailwind.css`; used by `OwnerDashboard.jsx`.

## Tests

- `backend/tests/test_owner_audit_logs.py` — **new**. Purpose: backend regression coverage for owner audit severity classification, role-diff “affected permissions,” `entity_id` filtering, and end-to-end audit-log payload shape for role-change events. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-15 01:20:13 -07:00`. Dependencies: imports `_severity_for_event` and `_affected_permissions_for_role_change` from `backend/app/api/owner_admin.py`, `capabilities_for_role` from `roles.py`, `PLATFORM_TENANT_ID`, `User`, `Tenant`, and `AuditLog`.
- `backend/tests/test_owner_platform_rbac_matrix.py` — **new**. Purpose: pure-function tests for the new RBAC foundation, asserting owner superuser behavior, platform-admin ceilings, denial for tenant/biller roles, access-level completeness, and `require_platform_permission` behavior. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 15:24:16 -07:00`. Dependencies: imports `require_platform_permission` from `role_guards.py`, `ACCESS_LEVEL_FOR_ROLE`, `PLATFORM_ROLES`, `access_level_for_role`, and `role_can` from `roles.py`, plus `CurrentUser` for dependency tests.
- `backend/tests/test_owner_platform_staff_delegation.py` — **new**. Purpose: API-level regression coverage for lifecycle transitions (`/status`, `/revoke-access`, `/remove`) and delegated permission grants (`/permissions`), including revocation, final-owner protection, and real effective-access changes after delegation. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:01:57 -07:00`. Dependencies: imports `PLATFORM_TENANT_ID`, `create_access_token`, `StaffPermissionGrant`, `AuditLog`, `Tenant`, and `User`; exercises `backend/app/api/owner_admin.py` endpoints and `roles.py` grant logic.
- `backend/tests/test_owner_platform_staff_lifecycle.py` — **new**. Purpose: broader staff-lifecycle regression suite covering account types, owner counts, role changes, account-type filters, job titles, platform assignments, identity-accountability fields, and owner-continuity signals returned by the recovered API. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 20:57:24 -07:00`. Dependencies: imports `PLATFORM_TENANT_ID`, `create_access_token`, `AuditLog`, `Tenant`, and `User`; exercises create/list/detail/role/audit endpoints in `owner_admin.py` and model fields in `user.py`.
- `backend/tests/test_owner_platform_staff_profile.py` — **new**. Purpose: API regression coverage for creating SNS staff, viewing SNS staff detail, and editing staff profiles, with explicit assertions that tenant users are out of scope and that platform-admin and auditor ceilings are enforced. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 16:16:37 -07:00`. Dependencies: imports `PLATFORM_TENANT_ID`, `create_access_token`, `AuditLog`, `Tenant`, and `User`; drives the recovered staff profile endpoints in `owner_admin.py`.
- `backend/tests/test_treatment_identity_migration.py` — **modified**. Purpose: existing ontology/treatment migration regression suite remains unchanged except for `HEAD_REVISION = "um2a1c2c3o5u6"`, aligning the migration test’s expected head to the first recovered staff migration so downgrade/re-upgrade comparisons still land on the recovered chain. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:11:23 -07:00`. Dependencies: imports Alembic `command`, `Config`, `ScriptDirectory`, SQLAlchemy engine/text helpers, `scoped_env_vars`, and `TEST_DATABASE_URL`; indirectly depends on the Alembic revision chain.

## Frontend UI

- `sns-emr-frontend/public/brand/sns-logo-dark.svg` — **modified**. Purpose: dark-treatment full SNS Hospice Solutions logo asset; the recovered variant updates the artwork used by `BrandLogo` for light-background contexts. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:11:23 -07:00`. Dependencies: referenced through `sns-emr-frontend/src/components/BrandLogo.tsx` and then consumed by login/set-password/reporting pages.
- `sns-emr-frontend/public/brand/sns-logo-icon-dark-tile.svg` — **new**. Purpose: tightly cropped dark icon asset introduced specifically for compact square tiles; the recovered `BrandLogo` comments explain it removes the large transparent margin present in the original 80x80 icon so the glyph fills the sidebar tile. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 17:41:12 -07:00`. Dependencies: referenced only by `BrandLogo.tsx`, then used by `OwnerShell.jsx` for the sidebar badge.
- `sns-emr-frontend/public/brand/sns-logo-icon-dark.svg` — **new**. Purpose: dark-treatment icon-only SVG variant added to the brand set so icon-only surfaces can use a dark-stroked mark independent of the original white-stroked icon. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 15:40:36 -07:00`. Dependencies: referenced by `BrandLogo.tsx` as variant `icon-dark`.
- `sns-emr-frontend/public/brand/sns-logo-icon.svg` — **modified**. Purpose: existing icon asset remains the generic fallback logo image, but the recovered version is now one member of a larger centralized brand-asset set managed by `BrandLogo.tsx`. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:11:23 -07:00`. Dependencies: referenced by `BrandLogo.tsx` as the `icon` variant and on image-load fallback.
- `sns-emr-frontend/public/brand/sns-logo-light.svg` — **modified**. Purpose: light-treatment full logo asset for dark backgrounds, coordinated with the recovered `BrandLogo` single-source-of-truth component. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:11:23 -07:00`. Dependencies: referenced by `BrandLogo.tsx` and pages that request the `light` variant.
- `sns-emr-frontend/src/App.tsx` — **modified**. Purpose: frontend router remains otherwise unchanged but now imports `SetPasswordPage` and registers `/set-password`, exposing the public reset-link flow minted by recovered owner staff creation/reset endpoints. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 22:06:01 -07:00`. Dependencies: imports `SetPasswordPage.tsx`; still owns top-level routing for `OwnerDashboard`, `TenantDashboard`, and billing pages.
- `sns-emr-frontend/src/components/BrandLogo.tsx` — **modified**. Purpose: converts duplicated raw `<img src="/brand/...">` usage into a single logo component with explicit variants (`light`, `dark`, `icon`, `icon-dark`, `icon-dark-tile`) and image-error fallback to the icon asset. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 21:11:23 -07:00`. Dependencies: imports only React CSS typings; used by `OwnerShell.jsx`, `SetPasswordPage.tsx`, `LoginPage.tsx`, `TenantDashboard.jsx`, `PortalShell.tsx`, `Sidebar.tsx`, `SNSAnalytics.tsx`, and `HopeReport.jsx` in the recovered tree.
- `sns-emr-frontend/src/owner/accessLevels.js` — **new**. Purpose: presentation-only mapping from backend-derived access-level codes (`LEVEL_1_OWNER`, etc.) to user-facing labels, with comments explicitly stating it must never compute permissions or derive levels from roles locally. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 20:05:22 -07:00`. Dependencies: imported by `UserManagement.jsx`, `AddStaffModal.jsx`, and `StaffProfileDrawer.jsx`; backed by `access_levels_by_role` / `access_level` values from the owner API.

## Owner Audit Log

- `sns-emr-frontend/src/owner/auditCategories.js` — **new**. Purpose: shared metadata for audit category pills and severity styling, mirroring the backend’s `VALID_AUDIT_CATEGORIES`/severity outputs without attempting to reclassify events locally. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-15 01:02:55 -07:00`. Dependencies: imported by `AuditLogs.jsx` and `AuditEventDrawer.jsx`; values are paired with `severity` and `category` returned by `sns-emr-frontend/src/api/ownerAdmin.ts`.
- `sns-emr-frontend/src/owner/components/AuditEventDrawer.jsx` — **new**. Purpose: right-side event-details drawer for the standalone audit page that renders overview, before/after state, affected permissions, request ID, and related events fetched by `entity_id`, with comments carefully distinguishing real API fields from derived metadata formatting. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-15 01:24:44 -07:00`. Dependencies: imports `CATEGORY_META`, `SEVERITY_META`, `IconClose`, and `fetchOwnerAuditLogs`; opened by `AuditLogs.jsx`.
- `sns-emr-frontend/src/owner/pages/AuditLogs.jsx` — **modified**. Purpose: upgrades the owner audit page into a richer standalone interface with category-count chips, date windows, tenant filter, CSV export, severity badges, and drawer-based event details/related-event browsing over the real `GET /api/owner/audit-logs` payload. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-15 01:19:18 -07:00`. Dependencies: imports `fetchOwnerAuditLogs`, `fetchOwnerTenants`, `CATEGORY_META`, `SEVERITY_META`, `AuditEventDrawer`, and shell icons; depends on backend fields added in `backend/app/api/owner_admin.py` such as `severity`, `affected_permissions`, `request_id`, and `entity_id` filtering.

## User Management

- `sns-emr-frontend/src/api/ownerAdmin.ts` — **modified**. Purpose: the existing owner API client keeps tenant and billing-provider calls, but the recovered change adds the client-side contract for staff/audit recovery features: audit severity/request-id/affected-permissions types plus functions for platform-staff list/detail/audit/profile/role/status/revoke/remove/password-reset and delegated-permission operations. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-15 01:18:39 -07:00`. Dependencies: imports token/session helpers from `./session` and `./client`; consumed by `OwnerDashboard.jsx`, `UserManagement.jsx`, `AddStaffModal.jsx`, `StaffProfileDrawer.jsx`, `AuditLogs.jsx`, `AuditEventDrawer.jsx`, `TenantManagement.jsx`, `DashboardOverview.jsx`, `BillingLicensing.jsx`, and `Settings.jsx`.
- `sns-emr-frontend/src/owner/components/AddStaffModal.jsx` — **new**. Purpose: shared “Add Staff / Invite Staff” modal for creating SNS platform staff or platform identities, with platform, department, job-title, role, account-type, and identity-accountability sections; comments explicitly note there is no separate pending-invite backend lifecycle yet. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 20:06:04 -07:00`. Dependencies: imports `createOwnerPlatformStaff` and `fetchOwnerPlatformUsers` from `ownerAdmin.ts`, shell icons, and `accessLevelLabel`; opened from `UserManagement.jsx`.
- `sns-emr-frontend/src/owner/components/StaffProfileDrawer.jsx` — **new**. Purpose: slide-out profile/access/security/audit panel for one SNS staff account, including edit mode, role-change actions, ownership transfer workflow, lifecycle actions, password reset, and platform-identity accountability fields; “coming in a future phase” placeholders are explicit where real services do not yet exist. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 22:04:23 -07:00`. Dependencies: imports multiple staff-management API functions from `ownerAdmin.ts`, shell icons, and `accessLevelLabel`; launched from `UserManagement.jsx`.
- `sns-emr-frontend/src/owner/pages/UserManagement.jsx` — **modified**. Purpose: this page is substantially repurposed from a broad cross-tenant user roster into SNS Staff & Access for platform personnel only, adding human/service/API tabs, audit tab, backend-driven row actions, owner-count/service-account/API KPIs, add/invite modal entry points, and profile drawers while removing the old tenant/agency filter emphasis. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 22:04:39 -07:00`. Dependencies: imports `fetchOwnerPlatformUsers`, `fetchOwnerAuditLogs`, `resetOwnerPlatformUserPassword`, `setOwnerPlatformUserActive`, `AddStaffModal`, `StaffProfileDrawer`, shell icons, and `accessLevelLabel`; rendered inside `OwnerDashboard.jsx` and backed by `backend/app/api/owner_admin.py`.

## Set Password

- `sns-emr-frontend/src/pages/SetPasswordPage.tsx` — **new**. Purpose: public password-set destination for admin-issued reset links; it validates the token via `validateSetPasswordToken`, submits via `setPasswordWithToken`, and uses the shared brand component for a polished standalone access-recovery page. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 22:05:57 -07:00`. Dependencies: imports `validateSetPasswordToken` and `setPasswordWithToken` from `../api/auth`, `BrandLogo` from `../components/BrandLogo`, and is routed by `sns-emr-frontend/src/App.tsx`.

## Authorization

- `sns-emr-frontend/src/utils/authorization.ts` — **modified**. Purpose: route-access helpers remain centralized, but the recovered edit deliberately broadens owner-route access from `user.role === "OWNER"` to any `user.access_scope === "platform"`, leaving fine-grained action control to backend `require_platform_permission()`/`allowed_actions`; comments explicitly warn against narrowing it back. Recovery source: live worktree `suasonns-psychic-adventure`, commit `6f3c8ac4` + uncommitted changes. Last modified: `2026-09-14 22:07:41 -07:00`. Dependencies: imports `SessionUser`; used by `OwnerDashboard.jsx`, `RequireRoleAccess` consumers, and general route/default-path checks.
